# Pipeline-level requirements.
# Install from the apple-silicon virtualenv (or a new venv at repo root).
#
# Core pipeline infrastructure
pyyaml>=6.0
python-dotenv>=1.0
# Dataset downloads (subset of what apple-silicon/requirements.txt already covers)
datasets>=2.14.0
huggingface_hub>=0.20.0
hf_transfer
# Stage 2 profiling
pandas>=2.0.0
pyarrow>=14.0.0
tiktoken>=0.3.0
# Stage 5 training (Apple Silicon only)
# mlx is pre-installed in the apple-silicon/.apple_env virtual environment.
# Install with: pip install mlx  (requires macOS 13.5+ on Apple Silicon)
mlx>=0.22.0
# Stage 7 inference playground (soft dependency — only needed for the Gradio UI)
gradio>=4.0.0
